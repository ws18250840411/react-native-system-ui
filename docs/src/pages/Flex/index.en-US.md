# Flex

### Introduction

`Flex` exposes a thin wrapper around the native flexbox primitives so you can build responsive layouts fast.

### Import

```jsx
import React from 'react'
import { Flex } from 'react-native-system-ui'
```
