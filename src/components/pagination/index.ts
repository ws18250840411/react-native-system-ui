import Pagination from './Pagination'

export type { PaginationProps, PaginationMode, PaginationPageItem } from './types'
export { usePaginationTokens } from './tokens'

export default Pagination
export { Pagination }
